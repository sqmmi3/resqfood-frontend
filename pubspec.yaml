name: frontend
description: "Waste less food."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  flutter_dotenv: ^6.0.0
  
  http: ^1.6.0

  json_annotation: ^4.9.0

  provider: ^6.1.5+1

  shared_preferences: ^2.5.3

  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets: 
    - assets/logo/
    - .env
